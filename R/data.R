#' Spatial data map of NUTS level 1 units of Poland 2016
#'
#' A `sf` map from `shp` with NUTS level 1 units of Poland 2016 with its
#' name and NUTS id's.
#'
#' @format A `sf` map with 7 rows and 3 variables:
#' \describe{
#'   \item{name}{name of unit}
#'   \item{id}{unit's NUTS id code}
#'   \item{geometry}{Shp coordinates}
#' }
"level1_map"

#' Spatial data map of NUTS level 2 units of Poland
#'
#' A `sf` map from `shp`` with NUTS level 2 units of Poland 2016 with its
#' name and NUTS id's.
#'
#' @format A `sf` map with 16 rows and 3 variables:
#' \describe{
#'   \item{name}{name of unit}
#'   \item{id}{unit's NUTS id code}
#'   \item{geometry}{Shp coordinates}
#' }
"level2_map"

#' Spatial data map of NUTS level 3 units of Poland
#'
#' A `sf` map from `shp` with NUTS level 3 units of Poland 2016 with its
#' name and NUTS id's.
#'
#' @format A `sf` map with 17 rows and 3 variables:
#' \describe{
#'   \item{name}{name of unit}
#'   \item{id}{unit's NUTS id code}
#'   \item{geometry}{Shp coordinates}
#' }
"level3_map"

#' Spatial data map of NUTS level 4 units of Poland
#'
#' A `sf` map from `shp` with NUTS level 4 units of Poland 2016 with its
#' name and NUTS id's.
#'
#' @format A `sf` map with 73 rows and 3 variables:
#' \describe{
#'   \item{name}{name of unit}
#'   \item{id}{unit's NUTS id code}
#'   \item{geometry}{Shp coordinates}
#' }
"level4_map"

#' Spatial data map of NUTS level 5 units of Poland
#'
#' A `sf` map from `shp` with NUTS level 5 units of Poland 2016 with its
#' name and NUTS id's.
#'
#' @format A `sf` map with 380 rows and 3 variables:
#' \describe{
#'   \item{name}{name of unit}
#'   \item{id}{unit's NUTS id code}
#'   \item{geometry}{Shp coordinates}
#' }
"level5_map"

#' Spatial data map of NUTS level 6 units of Poland
#'
#' A `sf` map from `shp` with NUTS level 6 units of Poland 2016 with its
#' name and NUTS id's.
#'
#' @format A `sf` map with 2478 rows and 3 variables:
#' \describe{
#'   \item{name}{name of unit}
#'   \item{id}{unit's NUTS id code}
#'   \item{geometry}{Shp coordinates}
#' }
"level6_map"
